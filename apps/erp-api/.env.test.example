# Test Environment Configuration
# Copy this file to .env.test and update with your test database credentials

# Test Database Configuration
# IMPORTANT: Use a separate database for testing to avoid data loss
# The test suite will run migrations and clean up data between tests
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/erp-test"

# Alternative: Use Neon for cloud-based testing (recommended)
# DATABASE_URL="postgresql://user:pass@ep-xxx.region.aws.neon.tech/erp-test"

# Server Configuration
PORT=8001
HOST=0.0.0.0
NODE_ENV=test

# Better Auth Configuration
# Must be at least 32 characters
BETTER_AUTH_SECRET="test-better-auth-secret-min-32-chars"
BETTER_AUTH_URL=http://localhost:8001
FRONTEND_URL=http://localhost:3000

# JWT Configuration
# Must be at least 32 characters
JWT_SECRET="test-jwt-secret-min-32-chars-for-testing"
JWT_EXPIRES_IN=24h

# Logging
LOG_LEVEL=error

# Testing Helpers
# Set to true to bypass auth middleware during integration tests
BYPASS_AUTH_FOR_TESTS=true

# Instructions:
# 1. Copy this file: cp .env.test.example .env.test
# 2. Create test database: createdb erp-test
#    Or use an existing Neon database
# 3. Update DATABASE_URL with your test database connection string
# 4. Run tests: pnpm test:integration
